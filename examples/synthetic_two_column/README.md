# Synthetic Two-Column Example

This directory is reserved for synthetic tests of the two-column Doppler retrieval model.

Use this example to validate the generic Doraex operators, marginalized likelihood, and map posterior reconstruction before running target-specific retrievals.

